# quiz
给你一个满足下述两条属性的 m x n 整数矩阵：

每行中的整数从左到右按非严格递增顺序排列。
每行的第一个整数大于前一行的最后一个整数。
给你一个整数 target ，如果 target 在矩阵中，返回 true ；否则，返回 false 。

## solution
这题虽然标成中等，但还是比较容易的，直接把这个二维矩阵一维化，然后就和昨天的题一样了。然后在leetcode社区还了解到一种做法，就是可以先垂直方向上二分，然后在水平方向二分，感觉也不错，留作之后再刷的时候写吧